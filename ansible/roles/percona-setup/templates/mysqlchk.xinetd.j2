service mysqlchk
{
        disable = no
        flags = REUSE
        socket_type = stream
        port = {{ mysqlchk_port }}
        wait = no
        user = nobody
        server = /usr/bin/clustercheck
        log_on_failure += USERID
        only_from = 0.0.0.0/0
        per_source = UNLIMITED
}