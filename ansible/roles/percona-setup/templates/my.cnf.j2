# {{ ansible_managed }}
{% set sections = default_config|combine(global_config,recursive=True) -%}
{% for section, config in sections.items() %}
[{{ section }}]
{% for name, value in config.items() %}
{% if value is not none %}
{{ name }} = {{ value }}
{% else %}
{{ name }}
{% endif %}
{% endfor %}

{% endfor %}
